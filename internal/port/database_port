package port

import (
	"context"

	domain "github.com/timpamungkas/grpc-go-server/internal/application/domain/dummy"
)

type DummyDatabasePort interface {
	Save(data *domain.Dummy) (uuid.UUID, error)
	// Save(str string) (string, error)
}
